configure_file(
  input: 'autobright.desktop.in',
  output: 'autobright.desktop',
  configuration: {
    'exec': prvdir / 'debugui' / 'app.js'
  },
  install: true,
  install_dir: datadir / 'applications'
)
